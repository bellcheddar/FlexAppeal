# Every option FlexAppeal exposes

Generated from `flexappeal/options.py` by `./FlexAppeal.py docs`.
Do not edit this file by hand; edit the registry and regenerate.

115 options across 14 groups, targeting OpenMM 8.5.

## 🏷️ Job

What this run is called and where its output lands.

| Option | Type | Default | Units | Applies when | Description |
|---|---|---|---|---|---|
| `job_name` | text | flexappeal_run | — | always | Used for the bundle filename, the output directory and the title on every analysis plot. Letters, digits, hyphens and underscores only, because it becomes a path on the user's machine. |
| `job_description` | textarea | — | — | always | Free text carried verbatim into the results manifest. Six months from now this is the only thing that will tell you why you ran this. |
| `job_author` | text | — | — | always | Recorded in the manifest for provenance. Optional. |
| `output_dir` ⚙️ | text | ./flexappeal_output | — | always | Where the run writes on your machine, relative to wherever you put the bundle. Everything the run produces stays inside this one directory so it is trivial to archive or delete. |

## 🧬 Input structure

The starting coordinates and what to keep from them.

| Option | Type | Default | Units | Applies when | Description |
|---|---|---|---|---|---|
| `input_source` | select | upload | — | always | Where the starting coordinates come from. Fetching by ID is reproducible and records the exact accession in the manifest; uploading is right for anything unreleased, modelled or hand-edited. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `upload` | choice | | |  | *Upload a file* — PDB or PDBx/mmCIF, up to 25 MB. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `rcsb` | choice | | |  | *Fetch from the RCSB PDB* — By four-character PDB ID. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `opm` | choice | | |  | *Fetch from OPM (pre-oriented)* — The Orientations of Proteins in Membranes database returns the structure already positioned in a bilayer frame, which is exactly what addMembrane needs. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `alphafold` | choice | | |  | *Fetch from the AlphaFold DB* — By UniProt accession. |
| `input_file` | file | — | — | `input_source == 'upload'` | A PDB or PDBx/mmCIF file. It is parsed server-side only to populate the options below; the copy embedded in your bundle is the sanitised version produced by the choices you make here. |
| `pdb_id` | pdbid | — | — | `input_source in ('rcsb', 'opm')` | Four characters, for example 1AKI. Fetched over HTTPS when the bundle is built, not at run time, so your run does not depend on the RCSB being up. |
| `uniprot_id` | text | — | — | `input_source == 'alphafold'` | For example P00698. Retrieves the AlphaFold model; remember that predicted structures have no waters, no ligands and unreliable loop conformations. |
| `assembly` | select | asymmetric | — | `input_source in ('rcsb', 'opm')` | Crystal structures are deposited as an asymmetric unit, which is often not the biological molecule. If your protein is a physiological dimer, simulating one chain is simulating the wrong thing. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `asymmetric` | choice | | |  | *Asymmetric unit* — Exactly what is in the deposited file. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `biological` | choice | | |  | *First biological assembly* — Built from the deposited transformation matrices. |
| `model_index` ⚙️ | int | 1 | — | always | NMR ensembles and some predictions contain many models. Model 1 is conventionally the representative one. |
| `chains` | multiselect | * | — | always | Populated from the structure once it is loaded. Dropping chains you do not need is the single cheapest way to make a simulation faster. |
| `altloc` ⚙️ | select | occupancy | — | always | Crystal structures often model two conformations for the same side chain. OpenMM cannot simulate both, so exactly one has to be chosen. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `occupancy` | choice | | |  | *Highest occupancy* — Whichever the crystallographer refined as dominant. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `first` | choice | | |  | *First listed* — Usually altloc A. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `a` | choice | | |  | *Always altloc A* —  |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `b` | choice | | |  | *Always altloc B* —  |
| `keep_waters` | checkbox | on | — | always | Buried and bridging waters are frequently structurally important, and deleting them can collapse an active site during equilibration. Bulk waters are replaced by the solvation step regardless. |
| `water_shell` ⚙️ | number | 5.0 | Å | `keep_waters` | Only crystallographic waters within this distance of the protein are kept. Anything further out is bulk solvent that addSolvent will regenerate anyway. |
| `keep_heteroatoms` | multiselect | — | — | always | Cofactors, ligands, metals and modified residues found in the file. Anything kept here must have force-field parameters, which is what the ligand section below is for. Anything not listed is deleted. |
| `strip_hydrogens` ⚙️ | checkbox | on | — | always | Deposited hydrogen positions are usually either absent or placed by a different program with different conventions. Re-adding them with PDBFixer at a known pH is more consistent than trusting what arrived. |
| `disulfides` | select | auto | — | always | Getting these wrong changes the fold. Automatic detection finds SG–SG pairs closer than the cutoff below, which is reliable for well-resolved structures. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `auto` | choice | | |  | *Detect automatically* — By SG–SG distance. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `manual` | choice | | |  | *Specify manually* — Enter the residue pairs yourself. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `none` | choice | | |  | *None* — Treat every cysteine as free thiol. |
| `disulfide_cutoff` ⚙️ | number | 2.5 | Å | `disulfides == 'auto'` | An S–S bond is about 2.05 Å; 2.5 Å catches slightly strained or lower-resolution geometry without picking up unbonded neighbours. |
| `disulfide_pairs` | textarea | — | — | `disulfides == 'manual'` | One pair per line as CHAIN:RESID-CHAIN:RESID, for example A:6-A:127. |
| `termini_caps` ⚙️ | select | charged | — | always | Charged termini are correct for a complete protein. Neutral ACE/NME caps are correct for a fragment or a single domain excised from something larger, where an artificial charge would distort the local electrostatics. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `charged` | choice | | |  | *Charged (NH₃⁺ / COO⁻)* — The right choice for an intact protein. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `capped` | choice | | |  | *Capped (ACE / NME)* — The right choice for a fragment. |

## 🩹 Structure repair

PDBFixer: gaps, missing atoms, protonation.

| Option | Type | Default | Units | Applies when | Description |
|---|---|---|---|---|---|
| `fix_missing_residues` | checkbox | on | — | always | Disordered loops are absent from most crystal structures. Leaving a gap means simulating a chain break, which is almost never what you want; PDBFixer rebuilds them, though the conformation it invents is a guess. |
| `max_gap_length` ⚙️ | int | 15 | residues | `fix_missing_residues` | Rebuilt loops longer than about fifteen residues are essentially fiction and will dominate your RMSD. Above this, the run stops and tells you rather than quietly modelling something unusable. |
| `build_terminal_residues` ⚙️ | checkbox | off | — | `fix_missing_residues` | Unresolved termini are usually genuinely disordered rather than merely unmodelled. Adding a flapping tail costs atoms and adds noise to global metrics without adding information. |
| `replace_nonstandard` | checkbox | on | — | always | Selenomethionine, phosphoserine and similar have no parameters in the standard force fields. PDBFixer swaps them for their closest standard equivalent, which for MSE→MET is exact and for a phospho-residue is not. |
| `add_missing_atoms` | checkbox | on | — | always | Partially resolved side chains are common at the surface. Without this the force field will refuse to build a template for them. |
| `ph` | number | 7.4 | — | always | Sets the protonation of ionisable side chains. 7.4 is physiological; use the pH of your actual experiment if you are comparing to one, since a histidine flipping protonation state can change a binding site entirely. |
| `protonation_overrides` ⚙️ | textarea | — | — | always | One per line as CHAIN:RESID=STATE, for example A:64=HIP. Overrides the pH rule for individual residues, which matters most for catalytic histidines and buried carboxylates whose real pKa is nowhere near the model value. Valid states: HID, HIE, HIP, ASH, GLH, LYN, CYM, CYX. |
| `mutations` ⚙️ | textarea | — | — | always | One per line as CHAIN:WT-RESID-MUT using three-letter codes, for example A:ALA-57-GLY. Applied before any repair, so a rebuilt loop reflects the mutant. |

## ⚛️ Force field

The physics. Protein, water, lipid and ligand parameters.

| Option | Type | Default | Units | Applies when | Description |
|---|---|---|---|---|---|
| `protein_ff` | select | amber14-all.xml | — | always | ff14SB (inside amber14-all) is the most widely validated modern protein force field and the safe default. CHARMM36m is its main rival and is the better choice for disordered regions and for membrane work, where the lipid parameters are natively matched. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `amber14-all.xml` | choice | | |  | *AMBER ff14SB* — The default. Well validated for folded globular proteins. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `amber/protein.ff19SB.xml` | choice | | |  | *AMBER ff19SB* — Improved amino-acid-specific backbone profiles. Designed for and effectively requires OPC water, which is selected automatically below. Ships with the openmmforcefields package rather than OpenMM itself, so choosing it adds that dependency to your bundle even for an apo run. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `charmm36.xml` | choice | | |  | *CHARMM36m* — Better balanced for intrinsically disordered proteins, and the native partner for the CHARMM36 lipid parameters used by the membrane builder. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `amber99sbildn.xml` | choice | | |  | *AMBER ff99SB-ILDN* — The previous generation. Use for continuity with older work. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `amber03.xml` | choice | | |  | *AMBER ff03* —  |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `amber10.xml` | choice | | |  | *AMBER ff10* —  |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `amber96.xml` | choice | | |  | *AMBER ff96* —  |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `amoeba2018.xml` | choice | | |  | *AMOEBA 2018* — Polarisable. Far more accurate in principle and roughly two orders of magnitude slower in practice; only viable for very small systems. |
| `water_model` | select | tip3p | — | `solvent_mode == 'explicit'` | Must be compatible with the protein force field, and the app enforces that. TIP3P is the AMBER default and by far the cheapest. OPC and TIP4P-Ew reproduce bulk water properties considerably better at roughly 1.3× the cost, and matter most when you care about solvation or disordered states. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `tip3p` | choice | | | `protein_ff != 'charmm36.xml'` | *TIP3P* — Three sites. The default partner for the AMBER force fields. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `tip3pfb` | choice | | | `protein_ff != 'charmm36.xml'` | *TIP3P-FB* — Force-balance reparameterised TIP3P: better density and dielectric at the same cost. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `spce` | choice | | | `protein_ff != 'charmm36.xml'` | *SPC/E* — Three sites, better bulk properties than plain TIP3P. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `tip4pew` | choice | | | `protein_ff != 'charmm36.xml'` | *TIP4P-Ew* — Four sites. Excellent bulk water; the extra site costs about 25%. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `tip4pfb` | choice | | | `protein_ff != 'charmm36.xml'` | *TIP4P-FB* — Force-balance four-site model. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `opc` | choice | | | `protein_ff != 'charmm36.xml'` | *OPC* — Four sites. The best-performing rigid model for biomolecular work, and required by ff19SB. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `opc3` | choice | | | `protein_ff != 'charmm36.xml'` | *OPC3* — Three-site cousin of OPC: most of the accuracy at TIP3P cost. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `charmm_tip3p` | choice | | | `protein_ff == 'charmm36.xml'` | *CHARMM-modified TIP3P* — TIP3P with Lennard-Jones terms on the hydrogens. The only correct partner for CHARMM36. |
| `solvent_mode` | select | explicit | — | always | Explicit water is the physically correct choice and what almost all published protein MD uses. Implicit solvent is perhaps ten times faster and useful for long-timescale conformational sampling, but it misrepresents solvent-mediated interactions and salt effects. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `explicit` | choice | | |  | *Explicit solvent* — A real box of water molecules and ions. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `implicit` | choice | | |  | *Implicit solvent (GB)* — A continuum dielectric. Much faster, much less accurate. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `vacuum` | choice | | |  | *Vacuum* — No solvent at all. For testing and for gas-phase questions only. |
| `implicit_model` | select | implicit/gbn2.xml | — | `solvent_mode == 'implicit'` | GBn2 is the most accurate of the generalised Born variants OpenMM ships and the one to use unless you are reproducing older work. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `implicit/gbn2.xml` | choice | | |  | *GBn2* — The recommended generalised Born model. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `implicit/obc2.xml` | choice | | |  | *OBC2* — Onufriev-Bashford-Case II. The long-standing default elsewhere. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `implicit/obc1.xml` | choice | | |  | *OBC1* —  |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `implicit/gbn.xml` | choice | | |  | *GBn* —  |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `implicit/hct.xml` | choice | | |  | *HCT* — The oldest and least accurate. |
| `implicit_salt` | number | 0.15 | M | `solvent_mode == 'implicit'` | Screens electrostatics via the Debye-Hückel term. 0.15 M approximates physiological ionic strength. |
| `solute_dielectric` ⚙️ | number | 1.0 | — | `solvent_mode == 'implicit'` | The interior dielectric constant. 1.0 is the standard choice; raising it is a crude way to mimic electronic polarisation. |
| `solvent_dielectric` ⚙️ | number | 78.5 | — | `solvent_mode == 'implicit'` | The exterior dielectric constant. 78.5 is water at room temperature. |
| `ligand_ff` | select | openff-2.2.1 | — | `has_ligands` | Applies to every ligand and cofactor kept above. OpenFF Sage is the current best general-purpose small-molecule force field and is actively maintained. GAFF2 is the classical AMBER partner and the safer choice if you need to match older AMBER work. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `openff-2.2.1` | choice | | |  | *OpenFF 2.2.1 (Sage)* — The recommended modern choice. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `openff-2.0.0` | choice | | |  | *OpenFF 2.0.0 (Sage)* — For continuity with work published against 2.0. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `gaff-2.11` | choice | | |  | *GAFF 2.11* — The classical AMBER general force field. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `espaloma-0.3.2` | choice | | |  | *Espaloma 0.3.2* — A graph-neural-network force field. Promising accuracy, much less field experience. |
| `ligand_charge_method` ⚙️ | select | am1bcc | — | `has_ligands` | AM1-BCC is the standard and what both GAFF2 and OpenFF were parameterised against; it takes a minute or two per ligand. Gasteiger is instant and noticeably worse, and should only be used to get a pipeline working. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `am1bcc` | choice | | |  | *AM1-BCC* — The correct choice. Requires AmberTools, which the bundle installs. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `am1bccelf10` | choice | | |  | *AM1-BCC ELF10* — Conformer-averaged AM1-BCC. Slower, more reproducible. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `gasteiger` | choice | | |  | *Gasteiger* — Fast and crude. For smoke-testing only. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `from_file` | choice | | |  | *Take from the input file* — Use charges already present in the SDF/MOL2. |
| `has_ligands` ⚙️ | checkbox | off | — | always | Set automatically when you keep a heteroatom above. Turning it on reveals the small-molecule parameterisation options. |
| `extra_ff_xml` ⚙️ | file | — | — | always | An OpenMM force-field XML appended after the ones selected above, for custom residue templates or parameters you have derived yourself. |

## 💧 Solvation and box

Explicit water, counter-ions and periodic box.

| Option | Type | Default | Units | Applies when | Description |
|---|---|---|---|---|---|
| `box_shape` | select | dodecahedron | — | `solvent_mode == 'explicit' and not use_membrane` | A rhombic dodecahedron encloses the same minimum-image distance in about 71% of the volume of a cube, so roughly 30% fewer water molecules for identical physics. The only reason to prefer a cube is a downstream tool that cannot read triclinic boxes. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `dodecahedron` | choice | | |  | *Rhombic dodecahedron* — The efficient default: ~29% fewer waters than a cube. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `octahedron` | choice | | |  | *Truncated octahedron* — Almost as efficient; the traditional AMBER choice. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `cube` | choice | | |  | *Cube* — Simplest, largest, slowest. Choose it only if something downstream demands it. |
| `box_sizing` ⚙️ | select | padding | — | `solvent_mode == 'explicit' and not use_membrane` | Padding scales with the molecule and is what you want in almost every case. Explicit vectors are for reproducing someone else's box exactly. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `padding` | choice | | |  | *Solvent padding* — A minimum solvent shell around the solute. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `vectors` | choice | | |  | *Explicit box vectors* — Give the three box lengths yourself. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `num_waters` | choice | | |  | *Fixed water count* — Add exactly this many water molecules. |
| `padding` | number | 1.2 | nm | `solvent_mode == 'explicit' and box_sizing == 'padding' and not use_membrane` | The minimum distance from any solute atom to the box edge. It must exceed the non-bonded cutoff, or a molecule can interact with its own periodic image -- a physically meaningless artefact that is easy to miss in the output. The default leaves 0.2 nm of headroom above the default 1.0 nm cutoff, because the box shrinks under the barostat during equilibration. |
| `box_vectors` | text | — | nm | `solvent_mode == 'explicit' and box_sizing == 'vectors'` | Three lengths in nanometres, comma separated, for example 6.0, 6.0, 6.0. |
| `num_waters` | int | 10000 | — | `solvent_mode == 'explicit' and box_sizing == 'num_waters'` | Exact water count. The box is sized to accommodate them. |
| `positive_ion` | select | Na+ | — | `solvent_mode == 'explicit'` | The cation used for neutralisation and for reaching the target ionic strength. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `Na+` | choice | | |  | *Na⁺ (sodium)* — The default counter-ion for almost all biomolecular work. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `K+` | choice | | |  | *K⁺ (potassium)* — Closer to intracellular conditions than sodium. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `Li+` | choice | | |  | *Li⁺ (lithium)* —  |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `Rb+` | choice | | |  | *Rb⁺ (rubidium)* —  |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `Cs+` | choice | | |  | *Cs⁺ (caesium)* —  |
| `negative_ion` | select | Cl- | — | `solvent_mode == 'explicit'` | The anion used for neutralisation and for reaching the target ionic strength. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `Cl-` | choice | | |  | *Cl⁻ (chloride)* — The default counter-ion for almost all biomolecular work. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `Br-` | choice | | |  | *Br⁻ (bromide)* —  |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `F-` | choice | | |  | *F⁻ (fluoride)* —  |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `I-` | choice | | |  | *I⁻ (iodide)* —  |
| `ionic_strength` | number | 0.15 | M | `solvent_mode == 'explicit'` | Salt added on top of neutralisation. 0.15 M is roughly physiological. Zero gives you only the counter-ions needed to neutralise the net charge, which under-screens surface electrostatics. |
| `neutralize` ⚙️ | checkbox | on | — | `solvent_mode == 'explicit'` | A non-zero net charge under particle-mesh Ewald is handled by an implicit uniform background charge, which introduces real artefacts near the solute. There is essentially never a good reason to turn this off. |

## 🧱 Membrane

Lipid bilayer construction for membrane proteins.

| Option | Type | Default | Units | Applies when | Description |
|---|---|---|---|---|---|
| `use_membrane` | checkbox | off | — | always | Builds a bilayer around the protein using OpenMM's own addMembrane. The protein must already be oriented with the membrane normal along z, which is exactly what fetching from OPM gives you. |
| `lipid_type` | select | POPC | — | `use_membrane` | POPC is the standard general-purpose model membrane and the right default for a plasma-membrane protein. Mixed compositions are not supported by addMembrane; build those elsewhere and upload the result. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `POPC` | choice | | |  | *POPC* — Palmitoyl-oleoyl phosphatidylcholine. The usual choice. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `POPE` | choice | | |  | *POPE* — Phosphatidylethanolamine. Common for bacterial inner membranes. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `DOPC` | choice | | |  | *DOPC* — Dioleoyl phosphatidylcholine. More fluid than POPC. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `DPPC` | choice | | |  | *DPPC* — Dipalmitoyl phosphatidylcholine. Gel phase at room temperature. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `DMPC` | choice | | |  | *DMPC* — Dimyristoyl phosphatidylcholine. Short tails, thin bilayer. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `DLPC` | choice | | |  | *DLPC* — Dilauroyl phosphatidylcholine. Thinner still. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `DLPE` | choice | | |  | *DLPE* — Dilauroyl phosphatidylethanolamine. |
| `membrane_orientation` | select | opm | — | `use_membrane` | The bilayer is built in the xy plane at a fixed z, so a protein that is not already aligned to the membrane normal will be built into the membrane sideways. This is the most common way a membrane setup goes silently wrong. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `opm` | choice | | |  | *Fetch pre-oriented from OPM* — The reliable route. Uses the OPM database's own alignment. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `preoriented` | choice | | |  | *Already oriented* — Trust the input file's frame as given. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `principal` | choice | | |  | *Align principal axis to z* — A geometric guess. Works for a clean single bundle of transmembrane helices and not much else. |
| `membrane_center_z` ⚙️ | number | 0.0 | nm | `use_membrane` | Where the bilayer midplane sits along z, relative to the structure's own frame. Zero is correct for anything fetched from OPM. |
| `membrane_padding` | number | 1.0 | nm | `use_membrane` | Water thickness above and below the bilayer. Too little and the protein's extramembrane domains see their own periodic image through the solvent. |

## 🔧 System

Cutoffs, constraints and long-range electrostatics.

| Option | Type | Default | Units | Applies when | Description |
|---|---|---|---|---|---|
| `nonbonded_method` | select | PME | — | always | Particle-mesh Ewald is the only correct treatment of long-range electrostatics in a periodic box and is what essentially all modern biomolecular MD uses. The cutoff methods are appropriate only for implicit solvent or vacuum. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `PME` | choice | | | `solvent_mode == 'explicit'` | *PME* — Particle-mesh Ewald. The default for any periodic system. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `LJPME` | choice | | | `solvent_mode == 'explicit'` | *LJPME* — PME applied to dispersion as well as electrostatics. More accurate, ~15% slower. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `Ewald` | choice | | | `solvent_mode == 'explicit'` | *Ewald* — Exact but scales badly. For small reference calculations only. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `CutoffPeriodic` | choice | | |  | *Cutoff (periodic)* — Reaction-field style. Not appropriate for charged solutes. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `CutoffNonPeriodic` | choice | | | `solvent_mode != 'explicit'` | *Cutoff (non-periodic)* — For implicit solvent. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `NoCutoff` | choice | | | `solvent_mode != 'explicit'` | *No cutoff* — All pairs. Only tractable for small implicit-solvent or vacuum systems. |
| `nonbonded_cutoff` | number | 1.0 | nm | always | Where direct-space non-bonded interactions stop. 1.0 nm is standard for the AMBER force fields; CHARMM36 was parameterised at 1.2 nm with a switching function and should use that. Must be less than half the shortest box vector. |
| `use_switching` ⚙️ | checkbox | off | — | always | Smoothly tapers Lennard-Jones to zero before the cutoff instead of truncating it. Required for CHARMM36 as parameterised; unnecessary for AMBER, which uses a long-range dispersion correction instead. |
| `switch_distance` ⚙️ | number | 0.9 | nm | `use_switching` | Where the taper begins. Conventionally 0.1 nm inside the cutoff. |
| `ewald_error_tolerance` ⚙️ | number | 0.0005 | — | `nonbonded_method in ('PME', 'LJPME', 'Ewald')` | Controls the PME grid density. The default is accurate enough that reducing it further costs time without changing results. |
| `constraints` | select | HBonds | — | always | Constraining bonds to hydrogen removes the fastest vibrations in the system, which is what allows a 2 fs timestep instead of 0.5 fs. It is standard practice and has a negligible effect on everything people actually measure. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `HBonds` | choice | | |  | *Bonds to hydrogen* — The standard choice. Permits a 2 fs timestep. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `AllBonds` | choice | | |  | *All bonds* — Permits slightly longer steps; distorts some flexibility. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `HAngles` | choice | | |  | *All bonds and H-X-H angles* — Aggressive. Used with very long timesteps. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `None` | choice | | |  | *None* — Requires a timestep of 0.5 fs or less. |
| `rigid_water` ⚙️ | checkbox | on | — | `solvent_mode == 'explicit'` | Water models are parameterised as rigid bodies. Making them flexible is both slower and wrong relative to how the model was fitted. |
| `use_hmr` | checkbox | on | — | always | Moves mass from heavy atoms onto the hydrogens bonded to them, slowing the fastest remaining motions so the timestep can double to 4 fs. Total mass and equilibrium properties are unchanged. This is close to a free 2× speedup and is the single most effective performance option on this page. |
| `hydrogen_mass` ⚙️ | number | 1.5 | amu | `use_hmr` | The repartitioned hydrogen mass. 1.5 amu is the conservative, well-tested value that supports a 4 fs step; 4.0 amu permits longer steps but has been shown to perturb some kinetics. |
| `remove_cm_motion` ⚙️ | checkbox | on | — | always | Without this the whole system slowly acquires a net drift velocity, which steals kinetic energy from the thermostat and corrupts diffusion measurements. |
| `dispersion_correction` ⚙️ | checkbox | on | — | always | An analytic estimate of the Lennard-Jones energy beyond the cutoff. Needed for correct densities in constant-pressure simulations. Turn it off only when using a switching function, or you are counting the same energy twice. |

## ⏱️ Integrator

How the equations of motion are advanced.

| Option | Type | Default | Units | Applies when | Description |
|---|---|---|---|---|---|
| `integrator` | select | LangevinMiddle | — | always | LangevinMiddle is OpenMM's recommended integrator: it thermostats and integrates in one step, and its middle-scheme discretisation gives noticeably better configurational sampling at large timesteps than the older Langevin integrator. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `LangevinMiddle` | choice | | |  | *Langevin (middle scheme)* — The recommended default for constant-temperature MD. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `Langevin` | choice | | |  | *Langevin (leapfrog)* — The older scheme. Use for continuity with previous work. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `NoseHoover` | choice | | |  | *Nosé-Hoover* — Deterministic thermostat. Gives a true canonical ensemble without stochastic forces. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `Verlet` | choice | | |  | *Verlet* — Constant energy. Needs a separate thermostat, or none for NVE. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `Brownian` | choice | | |  | *Brownian* — Overdamped. For coarse-grained or implicit-solvent sampling. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `VariableLangevin` | choice | | |  | *Variable-timestep Langevin* — Adapts the step to the error tolerance. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `VariableVerlet` | choice | | |  | *Variable-timestep Verlet* —  |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `MTSLangevin` | choice | | |  | *Multiple-timestep Langevin* — Evaluates slow forces less often. Real speedups, and real care needed with the force grouping. |
| `temperature` | number | 310.0 | K | always | 310 K is physiological (37 °C). Use 298 K if you are comparing against room-temperature biophysical measurements. |
| `friction` | number | 1.0 | ps⁻¹ | `integrator in ('LangevinMiddle', 'Langevin', 'Brownian', 'VariableLangevin', 'MTSLangevin')` | How strongly the thermostat couples to the bath. 1 ps⁻¹ is the standard compromise: enough to control temperature, weak enough not to over-damp the dynamics. Values above about 5 ps⁻¹ noticeably slow real conformational motion. |
| `timestep` | number | 4.0 | fs | always | 4 fs is safe with hydrogen mass repartitioning and H-bond constraints, and is the default here because both are on. Without HMR use 2 fs; without any constraints use 0.5 fs. Too large a step shows up as a slow, steady rise in total energy rather than an immediate crash. |
| `error_tolerance` ⚙️ | number | 0.0001 | — | `integrator in ('VariableLangevin', 'VariableVerlet')` | The per-step error target for the variable-timestep integrators. |
| `use_thermostat` ⚙️ | checkbox | off | — | `integrator == 'Verlet'` | Only relevant with the Verlet integrator, which has no temperature control of its own. Langevin integrators already thermostat. |
| `collision_frequency` ⚙️ | number | 1.0 | ps⁻¹ | `use_thermostat` | How often the Andersen thermostat randomises velocities. |
| `random_seed` ⚙️ | int | 0 | — | always | Zero means OpenMM picks a fresh seed each run. Set a specific value to make a trajectory reproducible, though bitwise reproducibility also needs a fixed platform and deterministic forces. |

## 🎈 Pressure control

Constant-pressure coupling, if any.

| Option | Type | Default | Units | Applies when | Description |
|---|---|---|---|---|---|
| `barostat` | select | MonteCarlo | — | always | Production runs are normally NPT, so the density equilibrates to the correct value rather than being fixed by whatever the solvation step happened to produce. Membrane systems need the membrane barostat, which couples the bilayer plane and the normal separately. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `MonteCarlo` | choice | | | `not use_membrane` | *Monte Carlo (isotropic)* — The standard choice for a soluble protein in water. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `MonteCarloMembrane` | choice | | | `use_membrane` | *Monte Carlo (membrane)* — Scales the membrane plane and normal independently, with a surface-tension term. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `MonteCarloAnisotropic` | choice | | |  | *Monte Carlo (anisotropic)* — Independent pressure control per axis. For anisotropic systems that are not membranes. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `MonteCarloFlexible` | choice | | |  | *Monte Carlo (flexible)* — Allows the box angles to change. For crystal-like systems. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `none` | choice | | |  | *None (NVT)* — Constant volume. Correct once the density has already equilibrated. |
| `pressure` | number | 1.0 | bar | `barostat != 'none'` | 1 bar is atmospheric and what essentially every biomolecular simulation uses. |
| `barostat_frequency` ⚙️ | int | 25 | steps | `barostat != 'none'` | How many steps between volume-change attempts. 25 is OpenMM's default and is frequent enough to equilibrate the density without the Monte Carlo moves costing meaningful time. |
| `surface_tension` | number | 0.0 | bar·nm | `barostat == 'MonteCarloMembrane'` | Zero is the right choice for a normal bilayer: a tensionless membrane is what the CHARMM36 lipid parameters were designed to reproduce. Non-zero values are for deliberately stretching or compressing the membrane. |
| `membrane_xymode` ⚙️ | select | XYIsotropic | — | `barostat == 'MonteCarloMembrane'` | Whether the two in-plane dimensions scale together. Isotropic is correct for a homogeneous bilayer. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `XYIsotropic` | choice | | |  | *Isotropic in xy* — The two membrane-plane axes scale together. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `XYAnisotropic` | choice | | |  | *Anisotropic in xy* — The axes scale independently. |
| `membrane_zmode` ⚙️ | select | ZFree | — | `barostat == 'MonteCarloMembrane'` | Whether the membrane normal is allowed to fluctuate. Free is standard. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `ZFree` | choice | | |  | *Free* — The z dimension fluctuates independently. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `ZFixed` | choice | | |  | *Fixed* — The z dimension is held constant. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `ConstantVolume` | choice | | |  | *Constant volume* — z compensates for xy so total volume is conserved. |
| `anisotropic_pressure` ⚙️ | text | 1.0, 1.0, 1.0 | bar | `barostat == 'MonteCarloAnisotropic'` | Three pressures in bar for x, y and z. Equal values behave like the isotropic barostat but scale each axis independently, which lets a box become non-cubic in response to an anisotropic solute. |

## 📌 Restraints

Holding parts of the system in place.

| Option | Type | Default | Units | Applies when | Description |
|---|---|---|---|---|---|
| `use_positional_restraints` | checkbox | on | — | always | Holds the solute near its starting coordinates while the freshly added water relaxes around it. Without this, a structure can distort during the first few picoseconds simply because the solvent has not yet settled. The restraints are released in stages before production. |
| `restraint_selection` | select | protein_heavy | — | `use_positional_restraints` | Heavy atoms is the usual choice: it holds the fold without fighting hydrogen placement. Backbone-only allows side chains to relax, which is what you want if the side-chain packing came from a prediction. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `protein` | choice | | |  | *Protein* — All protein atoms including hydrogens. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `protein_heavy` | choice | | |  | *Protein heavy atoms* — Protein excluding hydrogens: half the size, and hydrogens are rarely analysed. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `backbone` | choice | | |  | *Backbone* — N, CA, C, O only. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `ca` | choice | | |  | *Cα only* — One atom per residue: the smallest useful representation. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `protein_ligand` | choice | | |  | *Protein and ligands* — Everything except water, ions and lipids. |
| `restraint_force` | number | 1000.0 | kJ/mol/nm² | `use_positional_restraints` | 1000 kJ/mol/nm² holds atoms within roughly 0.05 nm at 310 K: firm but not rigid. Much stiffer and minimisation struggles; much weaker and it is not really doing anything. |
| `restraint_schedule` ⚙️ | text | 1000, 500, 100, 10, 0 | kJ/mol/nm² | `use_positional_restraints` | Force constants applied in equal slices of the equilibration, from the first value down to the last. A stepped release lets the structure adapt gradually rather than being let go all at once. |
| `custom_restraints` ⚙️ | textarea | — | — | always | One per line as SEL1 \| SEL2 \| distance_nm \| force_constant, using MDTraj selection syntax. Applied throughout production, not just equilibration. |

## 📈 Protocol

Minimise, heat, equilibrate, produce.

| Option | Type | Default | Units | Applies when | Description |
|---|---|---|---|---|---|
| `minimize` | checkbox | on | — | always | Removes the steric clashes that solvation and hydrogen placement inevitably create. Skipping it on a solvated system will usually blow the run up in the first few steps. |
| `minimize_tolerance` ⚙️ | number | 10.0 | kJ/mol/nm | `minimize` | The force convergence criterion. 10 kJ/mol/nm is loose enough to be quick and tight enough that dynamics starts cleanly; there is no benefit in minimising hard, because the next step is to add thermal energy anyway. |
| `minimize_max_iterations` ⚙️ | int | 0 | — | `minimize` | Zero means run until the tolerance above is met, however long that takes. Set a ceiling only if a pathological starting structure is making minimisation run away rather than converge. |
| `heat_duration` | number | 100.0 | ps | always | Raises the temperature from near zero to the target in stages. A gradual ramp avoids the local hot spots that a sudden velocity assignment produces. |
| `heat_start_temp` ⚙️ | number | 50.0 | K | `heat_duration > 0` | Where the ramp begins. Starting from a low but non-zero temperature is gentler than starting from absolute zero. |
| `heat_stages` ⚙️ | int | 5 | — | `heat_duration > 0` | How many discrete temperature steps the ramp uses. |
| `equilibration_duration` | number | 1000.0 | ps | always | Constant-pressure equilibration with the restraints releasing. 1 ns is enough for the density and the solvation shell to settle for a typical globular protein; membrane systems usually need considerably longer, because the lipids have to reorganise around the protein. |
| `production_duration` | number | 100.0 | ns | always | The trajectory you will actually analyse. 100 ns is a reasonable first look at a globular protein's local dynamics; it is nowhere near enough to see a folding event or a large domain rearrangement. |
| `replicates` | int | 1 | — | always | Independent runs from the same starting structure with different velocity seeds. Three short replicates tell you far more about whether a result is real than one run three times as long, because they show you the variance. |
| `seed_strategy` ⚙️ | select | sequential | — | `replicates > 1` | How each replicate's random seed is chosen. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `sequential` | choice | | |  | *Sequential from the base seed* — Reproducible: seed, seed+1, seed+2. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `random` | choice | | |  | *Random per replicate* — Recorded in each replicate's manifest. |

## 💾 Output and reporting

What gets written, how often, and how big it will be.

| Option | Type | Default | Units | Applies when | Description |
|---|---|---|---|---|---|
| `traj_format` | select | xtc | — | always | XTC is compressed with a controlled precision loss and is typically three to four times smaller than DCD for coordinates that will only ever be analysed and viewed. DCD is uncompressed and universally readable. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `xtc` | choice | | |  | *XTC* — Compressed. The default: much smaller, lossy only in the third decimal place. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `dcd` | choice | | |  | *DCD* — Uncompressed, universally supported. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `hdf5` | choice | | |  | *HDF5 (MDTraj)* — Carries topology and unit cell inside the file. |
| `traj_interval` | number | 10.0 | ps | always | How often a frame is saved. This is the main determinant of output size. 10 ps gives 10,000 frames from a 100 ns run, which is ample for every metric on the analysis tab; sub-picosecond intervals are only needed for vibrational or fast-kinetics questions. |
| `traj_selection` | select | protein_ligand | — | always | Excluding water typically cuts the trajectory by 80-90%. Keep water only if you specifically need solvent structure, water-mediated contacts or diffusion. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `protein` | choice | | |  | *Protein* — All protein atoms including hydrogens. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `protein_heavy` | choice | | |  | *Protein heavy atoms* — Protein excluding hydrogens: half the size, and hydrogens are rarely analysed. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `backbone` | choice | | |  | *Backbone* — N, CA, C, O only. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `ca` | choice | | |  | *Cα only* — One atom per residue: the smallest useful representation. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `protein_ligand` | choice | | |  | *Protein and ligands* — Everything except water, ions and lipids. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `all` | choice | | |  | *Everything* — Including water, ions and lipids. Large. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `custom` | choice | | |  | *Custom selection* — An MDTraj selection expression, entered below. |
| `traj_custom_selection` | text | — | — | `traj_selection == 'custom'` | An MDTraj selection expression, for example 'protein or resname LIG'. |
| `enforce_periodic_box` ⚙️ | checkbox | on | — | always | Keeps every molecule inside the periodic box in the saved frames. Convenient for viewing, but it splits molecules that straddle a boundary, so the analysis step re-images the solute before computing anything geometric. |
| `state_interval` | number | 10.0 | ps | always | How often energies, temperature, pressure and density are written to the CSV. These are tiny, so log them often: they are your only diagnostic if a run goes wrong. |
| `state_fields` ⚙️ | multiselect | step, time, potentialEnergy, kineticEnergy, totalEnergy, temperature, volume, density, speed, progress, remainingTime | — | always | What goes into the CSV. The defaults cover every convergence check the analysis tab plots. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `step` | choice | | |  | *Step number* —  |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `time` | choice | | |  | *Simulated time* —  |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `potentialEnergy` | choice | | |  | *Potential energy* —  |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `kineticEnergy` | choice | | |  | *Kinetic energy* —  |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `totalEnergy` | choice | | |  | *Total energy* — The drift diagnostic: a steady rise means the timestep is too large. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `temperature` | choice | | |  | *Temperature* —  |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `volume` | choice | | |  | *Box volume* —  |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `density` | choice | | |  | *Density* — Should plateau near 1.0 g/mL for a solvated system. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `speed` | choice | | |  | *Speed (ns/day)* —  |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `progress` | choice | | |  | *Progress* —  |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `remainingTime` | choice | | |  | *Estimated time remaining* —  |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `elapsedTime` | choice | | |  | *Elapsed wall time* —  |
| `checkpoint_interval` | number | 100.0 | ps | always | How often a restart file is written. A long run will be interrupted at some point, and a checkpoint is the difference between resuming and starting again. |
| `save_system_xml` ⚙️ | checkbox | on | — | always | Writes the System, Integrator and final State as XML. This is what makes a run genuinely reproducible and lets you extend it later on different hardware without rebuilding anything. |
| `save_solvated_pdb` ⚙️ | checkbox | on | — | always | The complete prepared system before dynamics. Essential for diagnosing a preparation problem after the fact. |

## 🚀 Platform and performance

Which hardware back end runs the maths.

| Option | Type | Default | Units | Applies when | Description |
|---|---|---|---|---|---|
| `platform` | select | auto | — | always | OpenMM has no official Metal back end. On Apple Silicon the realistic choices are the multithreaded CPU platform and OpenCL, which does reach the GPU but is deprecated by Apple. Automatic benchmarking runs a few hundred steps on each available platform and picks the fastest, which takes under a minute and is more reliable than guessing. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `auto` | choice | | |  | *Benchmark and choose* — The recommended default. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `CPU` | choice | | |  | *CPU* — Multithreaded. Consistently fast on Apple Silicon's wide cores. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `OpenCL` | choice | | |  | *OpenCL* — Reaches the integrated GPU. Deprecated by Apple but functional on M-series. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `Metal` | choice | | |  | *Metal* — The third-party openmm-metal plugin. Not installed by default and not officially supported. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `CUDA` | choice | | |  | *CUDA* — NVIDIA only. Present for completeness if you run the bundle on a Linux box. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `Reference` | choice | | |  | *Reference* — The unoptimised reference implementation. For debugging only; roughly 100× slower. |
| `precision` | select | mixed | — | `platform in ('auto', 'OpenCL', 'CUDA', 'Metal')` | Mixed precision computes forces in single precision and accumulates in double. It is as accurate as full double precision for everything that matters here and considerably faster. Full double is only needed for energy-conservation tests. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `mixed` | choice | | |  | *Mixed* — The right choice. Single-precision forces, double-precision accumulation. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `single` | choice | | |  | *Single* — Fastest, and prone to visible energy drift in long runs. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `double` | choice | | |  | *Double* — Slowest. For NVE energy-conservation testing. |
| `cpu_threads` ⚙️ | int | 0 | — | `platform in ('auto', 'CPU')` | Zero lets OpenMM use every core. On an Apple Silicon machine with efficiency cores it is often faster to set this to the number of performance cores only, because the slowest thread paces the whole step. |
| `deterministic_forces` ⚙️ | checkbox | off | — | always | Forces identical results from identical input at a small performance cost. Needed only when you require bitwise reproducibility. |
| `device_index` ⚙️ | text | — | — | `platform in ('OpenCL', 'CUDA', 'Metal')` | Which GPU to use, when there is more than one. Blank means the first. |
| `memory_guard` ⚙️ | checkbox | on | — | always | Estimates the system's memory footprint and stops before starting if it would push the machine into swap. Swapping during MD does not merely slow the run down, it can extend a job from hours to days. |

## 📊 Analysis

What the local run computes before packing the results file.

| Option | Type | Default | Units | Applies when | Description |
|---|---|---|---|---|---|
| `analysis_metrics` | multiselect | rmsd, rmsf, rgyr, sasa, dssp, hbonds, contacts, pca, clusters | — | always | Computed on your machine against the full trajectory, then packed into the results file. Doing it here rather than server-side is what keeps the upload small and means nothing is lost to decimation. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `rmsd` | choice | | |  | *RMSD* — Backbone deviation from the reference over time. The first thing to look at. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `rmsf` | choice | | |  | *RMSF* — Per-residue fluctuation. Shows you which loops move. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `rgyr` | choice | | |  | *Radius of gyration* — Global compactness. Detects unfolding or collapse. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `sasa` | choice | | |  | *Solvent-accessible surface area* — Shrake-Rupley. Slow on long trajectories. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `dssp` | choice | | |  | *Secondary structure* — Per-residue DSSP assignment over time. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `hbonds` | choice | | |  | *Hydrogen bonds* — Baker-Hubbard occupancy analysis. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `saltbridges` | choice | | |  | *Salt bridges* — Charged-pair contact occupancy. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `contacts` | choice | | |  | *Contact map* — Residue-residue minimum distances. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `nativecontacts` | choice | | |  | *Native contacts (Q)* — Fraction of the starting structure's contacts retained. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `pca` | choice | | |  | *Principal component analysis* — Essential dynamics: the dominant collective motions. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `clusters` | choice | | |  | *Conformational clustering* — RMSD clustering with representative frames. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `ligand_rmsd` | choice | | | `has_ligands` | *Ligand RMSD* — Ligand pose stability after aligning on the protein. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `ligand_contacts` | choice | | | `has_ligands` | *Ligand contacts* — Per-residue contact occupancy with the ligand. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `membrane_scd` | choice | | | `use_membrane` | *Lipid order parameters* — Deuterium order parameters per tail carbon. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `membrane_apl` | choice | | | `use_membrane` | *Area per lipid* — The standard bilayer equilibration diagnostic. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `membrane_thickness` | choice | | | `use_membrane` | *Bilayer thickness* — Phosphate-to-phosphate distance. |
| `analysis_reference` ⚙️ | select | first | — | always | What RMSD and native contacts are measured against. The minimised structure is usually the more meaningful reference, because the first production frame has already drifted through equilibration. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `first` | choice | | |  | *First production frame* —  |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `minimized` | choice | | |  | *Minimised structure* — The recommended reference. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `input` | choice | | |  | *Input structure* — The original deposited coordinates, before any repair. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `average` | choice | | |  | *Trajectory average* — Useful for RMSF, misleading for RMSD. |
| `analysis_selection` ⚙️ | select | backbone | — | always | Which atoms are superposed before geometric metrics are computed. Backbone is standard; align on a single domain if the molecule has two that move relative to each other, or the RMSD will just report the hinge. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `protein` | choice | | |  | *Protein* — All protein atoms including hydrogens. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `protein_heavy` | choice | | |  | *Protein heavy atoms* — Protein excluding hydrogens: half the size, and hydrogens are rarely analysed. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `backbone` | choice | | |  | *Backbone* — N, CA, C, O only. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `ca` | choice | | |  | *Cα only* — One atom per residue: the smallest useful representation. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `protein_ligand` | choice | | |  | *Protein and ligands* — Everything except water, ions and lipids. |
| `custom_trackers` ⚙️ | textarea | — | — | always | One per line as NAME \| TYPE \| selections, where TYPE is distance, angle or dihedral. Selections use MDTraj syntax and are separated by semicolons. |
| `payload_tier` | select | standard | — | always | How much trajectory travels back with the metrics. Standard is right for almost everyone. Full is only needed if you expect to ask the web app for extra analyses later; the full trajectory always stays on your machine regardless of what you choose here. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `light` | choice | | |  | *Light (~5 MB)* — Metrics plus 100 frames. Enough to view and plot. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `standard` | choice | | |  | *Standard (~25 MB)* — Metrics plus 500 protein-only frames. The recommended default. |
| &nbsp;&nbsp;&nbsp;&nbsp;↳ `full` | choice | | |  | *Full (~150 MB)* — Metrics plus 2000 frames. Enables server-side re-analysis. |

## Legend

- ⚙️ collapsed behind **Advanced** in the form
- *Applies when* is the `requires` predicate: the field is hidden, skipped by the validator and omitted from the generated script whenever it evaluates false.
